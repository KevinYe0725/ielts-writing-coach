[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node /app/worker-runtime/dist/index.js"
preDeployCommand = "node /app/docker/migrate-with-lock.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
drainingSeconds = 60

# IELTS Writing 闭环训练 Web 项目 PRD

> 文档状态：需求草案 v0.4  
> 当前范围：IELTS Academic / General Training Writing Task 2  
> 核心决策：开源、自托管、UI 配置自有 AI、最长 60 分钟专项课、同步交付 Codex Skill

## 1. 产品定义

本产品不是单纯的 AI 作文批改网站，而是一套能够自动推动用户完成 IELTS Writing 学习闭环的 Web 训练系统。

项目从第一天按开源、自托管和可移植原则建设：核心功能不得依赖未公开的私有服务；用户能够在 UI 中连接自己的 AI 服务；个人用户可以一条命令在本地运行，也可以通过维护好的部署模板上线；不使用网页的用户可以通过随项目发布的 Codex Skill 完成同一教学闭环。

系统承担：

- 自动选题；
- 计时与保存；
- IELTS 四项估分与证据化批改；
- 从用户原文中选择高优先问题；
- 自动生成最长 60 分钟的专项训练；
- 自动安排延迟重写；
- 比较 Version 1 与 Version 2；
- 维护个人能力档案；
- 在后续陌生题中重新检测旧能力。
- 管理用户自己的 AI provider、模型路由和连接状态；
- 支持本地与云端自托管部署；
- 导出与 Codex Skill 兼容的学习档案。

用户只需要完成真正产生学习效果的动作：

- 独立写作；
- 主动判断与修正；
- 无提示生成；
- 微型段落写作；
- 延迟重写；
- 陌生题迁移。

核心原则：

> 流程可以自动化，学习不能被动化。

## 2. 产品目标

### 2.1 用户目标

- 降低“今天练什么、先改什么、何时重写”的管理成本；
- 将每篇作文暴露的问题转化为可以复用的写作能力；
- 减少同类语法、搭配、中式表达和论证问题在后续作文中的复发；
- 在限时、闭卷条件下逐步提高写作稳定性。

### 2.2 产品目标

- 首页始终只给用户一个明确的下一步；
- 每篇作文只突出最影响分数的能力瓶颈；
- 每个重点问题都进入专项练习、延迟重写和陌生题迁移；
- 用后续独立输出判断学习效果，而不是用“看完批改”或“做完题目”代替掌握。
- 不把用户锁定在某一家模型提供商或托管平台；
- Web 与 Codex Skill 使用同一份教学规则、能力分类和数据交换格式；
- 新用户在不了解服务器或命令行细节的情况下，也能完成首次部署和 AI 配置。

### 2.3 北极星指标

> 用户在后续独立写作中，不再重复已经学习过的高频问题的比例。

## 3. MVP 边界

### 3.1 MVP 包含

- Task 2 题库及自动选题；
- 40 分钟无辅助首写；
- 自动保存和异常恢复；
- TR、CC、LR、GRA 四项估分及原文证据；
- 每篇 1 个必选核心目标、0–1 个相关次要目标和 0–1 个旧目标复习项；
- 一个最长 60 分钟的专项训练学习块；
- 自动创建带日期的重写任务；
- 站内通知及一种外部提醒方式；
- 闭卷延迟重写；
- Version 1 / Version 2 对比；
- 个人能力档案和错误复发趋势；
- 数据导出与删除。
- OSI 认可的开源许可证、完整源代码和贡献 / 安全政策；
- UI 首次启动向导和自带 AI（BYOAI）配置；
- OpenAI 与自定义 OpenAI-compatible endpoint 适配器；
- 密钥仅会话使用或后端加密持久化两种模式；
- 一条命令启动的 Docker Compose 本地模式和至少一个维护中的云端一键部署模板；
- 自动迁移、健康检查、备份 / 恢复和版本升级说明；
- 仓库内可直接发现、可独立安装的 `coach-ielts-writing` Codex Skill；
- Web / Skill 学习档案导入导出兼容。

### 3.2 MVP 暂不包含

- Writing Task 1；
- 手写图片 OCR；
- 人工教师批改；
- 社区、排行榜和复杂游戏化；
- 原生移动 App；
- 语音答题；
- 班级和教师管理后台；
- 复杂知识图谱或贝叶斯掌握模型；
- 面向普通学员的多模型自由切换；管理员的实例级模型路由仍属于运维配置；
- 课程商城。
- 依赖官方托管后端才能工作的“开源外壳”；
- 浏览器直接携带 API key 调用模型；
- 企业级 SSO、外部 KMS 和多组织权限系统；
- 保证所有第三方或本地模型都达到同等 IELTS 评分质量；
- Codex Skill 的复杂可视化界面；
- 将 Skill 强制打包为公共插件市场产品，独立 Skill 先作为必交付物。

## 4. 完整训练闭环

### 阶段 1：自动选题

系统根据题型覆盖、话题覆盖、最近训练记录和个人弱点推荐一道题。用户允许有限次数换题，也可以粘贴自己的题目。

### 阶段 2：40 分钟 Version 1

- 构思、写作和检查均包含在 40 分钟内；
- 默认关闭拼写、语法和 AI 建议；
- 显示字数、剩余时间和 5 分钟 / 1 分钟提醒；
- 自动保存；
- 提交后原稿锁定，不允许覆盖修改；
- 被打断的写作可继续，但标记为非标准样本。

### 阶段 3：AI 批改与优先级收敛

批改输出：

- IELTS 四项估分与置信度；
- 每项判断对应的原文证据；
- 逐段论证分析；
- 逐句语法、搭配、自然度和逻辑诊断；
- 最影响分数的 2–3 个候选能力问题；
- 本篇可保留的表达和论证动作。

用户首屏只看到：

- 一分钟摘要；
- 最重要的训练目标；
- 已自动安排的专项训练时间。

如果用户刚完成 40 分钟首写，默认主按钮为“今天先到这里，专项训练已安排”；如果用户明确预留了连续学习时段，则显示“先做 3 分钟热身”。入口诚实显示预计总时长，但不用“开始 60 分钟任务”制造启动压力。

完整报告按需展开。Band 7 / 7.5 完整范文在 Version 2 提交前保持锁定。

### 阶段 4：60 分钟专项能力转化课

课程从用户本人的句子和论证问题出发，完成：

> 发现问题 → 澄清原意 → 理解差异 → 修复原句 → 独立生成 → 即时近迁移 → 段落应用 → 自检

详细规范见第 6–15 节。

### 阶段 5：自动安排延迟重写

- 专项课生成后即预创建重写任务，但此时只有建议学习窗口 `target_rewrite_at`，没有正式解锁时间；
- 核心路径完成后，以最后一次实质讲解、完整答案暴露或短回炉结束时间为锚点，生成至少 24 小时后的正式解锁时间 `due_at`；
- 默认在最后一次实质教学暴露后的 24–48 小时解锁；
- 自动落到用户下一个常用学习时段；
- 到期后首页主按钮自动变为“开始重写”；
- 允许一键推迟一天或移动到下一个学习时段；
- 允许软性提前解锁，但标记为“提前重写”；
- 未完成核心路径时只能称为“原计划重写窗口临近 / 已错过”，不能称为重写已到期；
- 用户可以主动跳过专项课并进入重写，但该版本标记为 `SKIPPED_PREREQUISITE`，不能作为专项训练保持效果的证据。

### 阶段 6：闭卷 Version 2

重写时隐藏：

- Version 1；
- 完整批改；
- 修改答案；
- 完整范文。

Version 2 分成两个可比较阶段：

1. 前 35 分钟只显示题目、正文和计时，不显示个人训练目标；
2. 最后 5 分钟展示最多三条抽象自检目标，允许用户检查和修改。

系统分别保存自检前版本和自检后版本。前者用于判断延迟独立应用，后者用于判断定向自检能力。抽象目标示例：

- 确保比较对象完整；
- 用自然搭配表达学业压力；
- 每个主体段补齐机制和结果。

### 阶段 7：版本比较与能力更新

Version 2 提交后自动比较：

- 四项估分变化；
- 旧问题是否复发；
- 目标表达是否自然使用；
- 论证链是否更完整；
- 用户能否在检查阶段自己发现问题；
- 用时、字数和限时稳定性；
- 新出现的高优先问题。

### 阶段 8：无提示迁移检测

在 D5–D7 的相邻新题或下一篇完整作文中，再次检测本轮目标。检测题必须自然提供使用该能力的机会，但题面不能提示目标词块或规则。只有跨时间、跨题目、无提示成功，能力才可以标记为已迁移；没有合理使用机会时记录为“无证据”，不能判失败。

## 5. 专项训练的课程决策

### 5.1 时长

- 用户为专项训练预留一个最长 60 分钟的学习块；
- 课程由 35–45 分钟核心路径和最多 15 分钟弹性路径组成；
- 核心路径包含一次 3 分钟微休息；
- 全课不允许因错误无限延长；
- 弹性路径仅在核心证据不足时自动用于补救；核心目标已经达标时，额外迁移或旧目标复习作为可选加练，不自动填满时间；
- 如果全部成功标准提前满足，可以在 45–60 分钟间结束；
- 到 60 分钟仍未稳定的目标，自动安排 8–20 分钟短回炉，不在当天无限追加。

计时口径：

- `planned_user_seconds = 3600`，包含计划内微休息；
- `lesson_clock_seconds` 只累计页面前台学习和计划内微休息，暂停、页面后台和 AI 等待不计入；
- `productive_seconds` 仅累计阅读必要材料和真实作答，用于分析，不用于惩罚用户；
- 到达 60 分钟时允许提交当前正在输入的答案，随后裁剪可选内容并结束。

### 5.2 目标数量

一节课程包含：

- 1 个必选核心新问题，承担全部严格通过标准；
- 0–1 个相关次要问题，仅当它与核心目标共享判断规则、可由同一输出证明，且前半程核心目标进展良好时加入；
- 0–1 个历史旧问题，只做无提示抽检，不影响核心目标是否通过。

Task Response 或 Coherence & Cohesion 作为核心目标时，默认不再加入新的次要目标。首次用户没有旧目标时，用核心目标的额外近迁移替代；没有可靠次要问题时不强行生成。

如果一篇作文存在四类以上严重问题，系统拆成多次课程，不在同一小时平均处理所有问题。

### 5.3 题量和主动输出比例

- 全课约 12–16 次有意义的输出；
- 识别、判断和选择类互动最多 3–4 道，且不超过任务量的 25%；
- 按题目配置的 `expected_active_seconds` 计算，主动输出不低于核心路径的 65%；主动输出包括解释、修复、造句、构建论证链、写段落和二次修改，不包括阅读反馈、点击和空闲停留；
- 核心目标至少包含 3 次首次无提示生成、2 个不同语境和 1 次综合输出；
- 一节课新增词块控制在 3–5 个；
- 完整讲解单次不超过 90 秒。

### 5.4 成功不是“做完题”

系统区分：

- **已完成**：完成课程核心路径；
- **临时通过 / applied**：能在本课无提示输出和即时近迁移；
- **延迟应用证据 / delayed application**：Version 2 自检前版本中未复发；
- **已保持 / retained**：Version 2 或其他任务在教学暴露至少 24 小时后，无目标提示、存在自然使用机会且独立输出成功；
- **已迁移 / transferred**：另一道题中也能独立使用。

60 分钟专项课最多将能力推进到 `applied`，不能直接宣称最终掌握。

## 6. 60 分钟标准课型

| 时间 | 阶段 | 用户动作 | 系统职责 |
|---|---|---|---|
| 0–5 分钟 | 闭卷前测 | 不看批改，修正本人旧句并确认表达意图 | 保存首次答案，建立真实基线 |
| 5–10 分钟 | 微型讲解 | 先预测，再查看规则和最小对比 | 只讲核心规则或表达视角，最多 3–5 个词块 |
| 10–18 分钟 | 辨析与修复 | 定位问题、匹配词块、解释并修复 | 逐级提示，错误后提供同构变式 |
| 18–27 分钟 | 递减支架输出 | 从句框到关键词，再到独立造句 | 逐步撤除提示，独立生成组批量反馈 |
| 27–30 分钟 | 微休息 | 离屏休息 | 自动保存，不展示知识卡片；切断对示范答案的短时模仿 |
| 30–41 分钟 | 即时近迁移 | 写新句、因果链或微型段落 | 换语境，只评价本课目标，避免满屏纠错 |
| 41–44 分钟 | 自检与出门测 | 修改综合答案，再完成未见表层形式的新题 | 保存自检前后版本，检查核心硬门槛 |
| 44–45 分钟 | 核心检查点 | 查看核心结果 | 达标则允许结束；未达标则进入弹性路径 |
| 45–57 分钟 | 弹性路径 | 完成定向补救；达标者可自愿做高阶近迁移或旧目标抽检 | 不自动填满时间，不创建超过上限的新分支 |
| 57–60 分钟 | 条件复测与排程 | 仅继续过弹性路径的用户完成新出门测 | 更新本课结果并自动安排短回炉和重写 |

不同问题类型必须保留：前测、主动生成、即时近迁移、自检和出门测，但采用不同时间模板：

- **语法 / 搭配模板**：短讲解、较多句级检索和重组，综合应用约 10–12 分钟；
- **TR / CC 模板**：将辨析与修复压缩为必要的结构诊断，减少配对和填空，把核心路径中的独立输出与应用合并为 18–20 分钟主体段写作。

第 50 分钟后不再创建新的补救分支，必须保留收尾、自检和出门测时间。

核心路径不可缺少：核心目标前测、一次受控修正、两个不同语境的首次无提示生成、一次综合近迁移、自检和未见表层形式的出门测。次要目标、旧目标和高阶加练均不是重写解锁的前置条件。

## 7. 训练页面呈现

### 7.1 单卡片交互

页面一次只呈现一个任务：

```text
自然表达 · 预计 2 分钟

你的原句：
The pressure from the courses in primary school
is much slighter.

任务：
你原本最想表达什么？

○ 小学生承受的学业压力较小
○ 小学生需要完成的课业较少
○ 小学课程本身要求较低

[我需要一点提示]                   [提交]
```

卡片公共区域：

- 当前阶段和预计用时；
- 用户原文证据；
- 一句明确任务说明；
- 作答区；
- “需要提示”和“提交”；
- 提交后的简短反馈；
- 可展开的“为什么”。

### 7.2 进度呈现

顶部统一显示五个用户可见阶段，而不是题目数量：

> 诊断 ✓　理解　独立输出　应用　收尾

微休息不构成学习阶段；句子近迁移、论证链和段落实验都属于“应用”。内部 `LessonBlock` 可更细，但必须映射到这五个界面阶段。

同时显示预计剩余时间和目标状态：

- 正在学习；
- 可以在提示下完成；
- 可以无提示输出；
- 需要后续复习。

不以单一百分比或大量正确题数代替能力进度。

### 7.3 键盘与移动端

- 所有拖拽交互同时支持点击配对；
- 桌面端支持键盘选择、提交和继续；
- 移动端适合完成短句、匹配和自检；
- 80–120 词段落建议桌面端完成，但移动端不强制禁止；
- 输入内容持续本地保存并同步到云端。

## 8. 专项题型目录

MVP 采用八类核心题型。它们按认知难度逐步出现，而不是随机混排。

### 8.1 原文聚光灯

**交互：** 用户在自己的原句中点选最值得检查的词或短语。  
**目的：** 重新注意到本人真实问题。  
**判分：** 确定性范围匹配，允许部分命中。

示例：

> The pressure from the courses in primary school is much slighter.

用户点选 `pressure from the courses` 和 `slighter` 后，系统反馈：

> `much + 比较级`本身正确。这里主要是搭配和表达视角问题：`slighter pressure`可以理解，但不够自然；`pressure from the courses`也没有清楚说明是谁承受压力。

问题标签必须区分：

- 硬性语法错误；
- 搭配 / 自然度问题；
- 中式信息组织；
- 可选优化。

不得把所有非最佳表达都标成“错误”。

### 8.2 含义岔路

**交互：** 用户选择自己原本真正想表达的意思。  
**目的：** 防止 AI 修改后改变原意。  
**判分：** 不计分，只决定课程分支。

| 用户意图 | 后续目标表达 |
|---|---|
| 学生承受压力 | `pupils face less academic pressure` |
| 课业量较少 | `pupils have a lighter workload` |
| 课程要求较低 | `primary-school courses are less demanding` |

允许用户选择“多个都相关”，但必须确定本句的首要重点。

### 8.3 表达地图

**交互：** 点击配对或拖拽语义与完整词块。  
**目的：** 建立“真实意思—英语表达视角—完整词块”的连接。  
**判分：** 确定性映射。

示例：

```text
学生承受压力     → face academic pressure
学生拥有课业量   → have a light/heavy workload
课程要求高低     → courses are less/more demanding
某事造成压力     → place pressure on somebody
```

反馈必须解释视角，不能只显示对错。

### 8.4 最小对比

**交互：** 从 2–4 个表达中选择最准确或最自然的一项，并说明原因。  
**目的：** 区分语法正确、意思精确和自然表达。  
**判分：** 经人工规则或自动校验后的确定性答案。

示例：

> 哪一句最自然地表达“小学生承受的学业压力较小”？

A. Primary-school pupils face slighter pressure.  
B. Primary-school pupils face less academic pressure.  
C. Primary-school courses give pupils lower pressure.

选择错误选项时，系统记录暴露出的混淆类型，而不只是记 0 分。

### 8.5 句子骨架重建

**交互：** 用户将完整词块放入语义槽位。  
**目的：** 训练句子结构和完整比较关系，而非逐词拼装。  
**判分：** 允许多种合法顺序的确定性判分。

```text
[谁] + [承受什么] + [与谁比较]

Primary-school pupils
generally face
less academic pressure
than secondary-school students
```

反馈按结构呈现：

```text
主体 ✓ → 动作与搭配 ✓ → 程度 ✓ → 比较对象缺失
```

### 8.6 限定条件改写 / 一意多写

**交互：** 用户输入完整句子。  
**目的：** 从支架输出过渡到独立生成。  
**判分：** AI 根据固定量表判断多种合理答案。

限定改写示例：

> 用“学生”作主语重写原句。必须使用 `face`，并写出完整比较对象。

一意三写示例：

> 分别从学生压力、课业量和课程要求三个角度表达“小学阶段的学习负担较轻”。

可接受答案包括：

- `Primary-school pupils generally face less academic pressure.`
- `Primary-school pupils generally have a lighter workload.`
- `Primary-school courses are generally less demanding.`

系统应明确说明三句表达的是不同事实，而不是简单同义替换。

### 8.7 论证链与桥梁句

**交互：** 角色分类、因果链排序、删除无关节点、补写机制句。  
**目的：** 训练观点、原因、机制、结果和意义之间的连接。  
**判分：** 排序和分类确定性判分；自由桥梁句由 AI 按量表评分。

示例论证链：

```text
课业量较轻
    ↓
有时间参加短时而规律的语言课程
    ↓
反复接触使基本语言模式逐渐熟悉
    ↓
建立初步语言基础
    ↓
日后学业更加繁重时，学习新语言更容易
```

系统可加入一个话题相关但不能推进本论证的干扰节点，用于检测用户是否真正理解逻辑。

### 8.8 段落实验室

**交互：** 用户在极简编辑器中完成 80–120 词微型段落，并进行一次定向自检和二次提交。  
**目的：** 在接近真实写作的环境中综合应用目标能力。  
**判分：** AI 只围绕本课目标解析和评分。

示例任务：

> 解释为什么小学学习外语可能有长期好处，同时承认它可能增加学生负担。要求使用一个自然的压力表达、至少三环因果链，并完成明确权衡。

结果不只显示总分，而要呈现：

```text
自然表达学业压力：已完成
核心观点：已出现
发生机制：已出现，但不够具体
直接结果：已完成
长期意义：缺失
权衡判断：已完成
```

系统高亮用户答案中的证据，并要求用户自行修订一次。与本课目标无关的小问题进入错误库，不在本题制造满屏红色批改。

## 9. 不同问题类型的题型组合

### 9.1 语法准确性

适合组合：

- 原文定位；
- 最小形式对比；
- 填空后完整重写；
- 句型转换或句子合并；
- 无提示造句；
- 微型段落中的定向语法自检。

每道受控题只测试一个主要语法点，避免同时引入大量生词和复杂逻辑。

### 9.2 搭配与中式英语

适合组合：

- 含义岔路；
- 表达地图；
- 自然度排序并解释；
- 一意多写；
- 根据不同主语重构；
- 跨话题无提示生成；
- 段落应用。

重点训练：

> 中文概念 → 确认真实语义 → 选择英语常用视角 → 调用完整词块。

### 9.3 Task Response 与论证

适合组合：

- 识别题型动作；
- 判断 thesis 是否完整回应问题；
- 标记观点、原因、机制、结果、权衡；
- 补齐 `Why / How / So what`；
- 因果链排序；
- 补写桥梁句；
- 陌生题主体段。

### 9.4 Coherence & Cohesion

适合组合：

- 句子功能标注；
- 乱序重排；
- 删除重复或无效连接句；
- 修复指代；
- 根据逻辑骨架扩写；
- 反向提纲。

系统不以连接词数量判断连贯性，而以读者能否还原清晰逻辑判断。

## 10. 提示、反馈与参考答案

### 10.1 提示阶梯

用户第一次作答前不显示答案。答错后采用三级提示：

1. **概念提示**  
   例如：“你描述的是谁承受压力，还是课程本身的要求？”
2. **局部支架**  
   例如：`Pupils ___ less academic pressure.`
3. **完整示范 + 立即变式**  
   显示 `Pupils face less academic pressure.` 后，立即要求用户表达“大学生承受更大的压力”。

如果用户看到完整示范后才答对，只能证明“理解了反馈”，不能计为无提示独立正确。

### 10.2 反馈分层

反馈最多包含四层，但按表现条件触发：

1. **即时状态**：一句话说明结果；
2. **用户证据**：高亮其答案中的具体位置；
3. **迁移规则**：总结可用于其他题目的判断规则；
4. **详细讲解**：仅在用户主动点击“为什么”后展开。

示例：

> 意思表达准确，但比较对象还不完整。

展开证据：

> `than older` 后缺少被比较的人群。

迁移规则：

> 写比较句时检查：A 与 B 是否属于同一类别，B 是否表达完整。

反馈节奏：

- 首次正确：只显示一句确认并进入下一题；
- 首次错误：显示最小提示和必要证据，允许一次修订；
- 第二次仍失败：展示参考表达和短变式；
- 相同迁移规则不逐题重复，在模块末统一总结。

### 10.3 开放题反馈

受控阶段可以逐题即时反馈；独立生成阶段应先连续完成 2–3 个答案，再统一反馈，避免第一题的答案泄露给后续题。开放题流程：

1. 用户第一次独立提交；
2. 独立生成组完成后，系统根据目标能力给最小提示；
3. 仅在存在目标问题时，用户自行修改并第二次提交；
4. 第二次仍失败时，系统展示完整反馈和一个参考表达；
5. 必要时生成一个不同语境的短变式；该变式只记为补救成功，不记为独立证据。

不要求用户答案与参考表达逐字一致。完整 Band 7 / 7.5 作文范文仍需等 Version 2 提交后开放。

### 10.4 AI 判断争议

用户可以点击“我的表达也可能成立”，系统重新评估：

- 是否存在其他自然语境；
- AI 是否错误理解用户意图；
- 是否属于“正确但不是当前训练目标”；
- 是否需要降低判分置信度或交由人工抽检数据集。

重新评估不能覆盖原始判断，必须保留版本和原因。

## 11. 判分机制

### 11.1 不计分任务

- 含义岔路；
- 自信度选择；
- 用户确认原意；
- 暂停原因标签。

这些数据决定分支或帮助诊断，但不进入正确率。

### 11.2 确定性判分

适用于：

- 原文范围定位；
- 词块匹配；
- 已校验的最小对比；
- 句子骨架；
- 固定答案填空；
- 论证角色分类；
- 因果链排序。

同样输入必须始终产生同样结果。选择题发布前必须校验答案唯一性和干扰项合理性。

### 11.3 AI 开放题判分

句子生成采用四分量表：

- 目标能力是否正确应用：0–2；
- 是否保持要求表达的语义：0–1；
- 句子整体是否可接受：0–1。

综合段落只围绕本课目标评分：

- 每个目标能力：0–2；
- 论证或语义完整度：0–2；
- 可理解性：0–2。

AI 必须返回：

- 各维度得分；
- 用户答案中的证据；
- 是否达到本题标准；
- 一条最重要、可执行的修改建议；
- 判分置信度。

### 11.4 首次答案与最终答案分离

每次练习必须分别保存：

- 首次答案；
- 是否使用提示；
- 使用到哪一级提示；
- 修改后答案；
- 是否查看参考答案；
- 最终正确性；
- 每次尝试用时。

掌握判断以首次独立答案和即时近迁移为主要证据，不用反复试错后的最终正确率虚高结果。

## 12. 自适应分支

### 12.1 前测稳定

如果前测无提示正确，且历史上不是高频复发问题：

- 跳过基础讲解和同级重复题；
- 进入独立生成；
- 保留无提示生成和综合应用以取得必要证据；
- 核心证据充分后结束核心路径；更难近迁移或旧目标复习作为可选加练。

### 12.2 受控题连续错误

连续两个受控题错误时：

- 插入更简单的最小对比；
- 给一张不超过 90 秒的微讲解卡；
- 加入一题同构变式；
- 每个目标最多追加两题。

### 12.3 能识别但不会生成

如果选择和修复题正确，但自由句子失败：

- 停止继续选择题；
- 切换为“完整句框 → 部分提示 → 关键词 → 无提示”；
- 记录为提取 / 生成问题，而非规则理解问题。

### 12.4 句子正确但段落失效

如果独立句子正确，但在段落中再次犯错：

- 安排限时段落变式；
- 启动目标自检清单；
- 要求二次修改；
- 标记为“尚未自动化”。

### 12.5 连续独立正确

连续两个主动输出或即时近迁移任务无提示正确时：

- 停止该目标的同级追加；
- 如果核心硬门槛尚缺综合任务或出门测，则进入这些必需环节；
- 核心证据充分后停止强制内容，可选择更难近迁移或直接结束；
- 不为凑满题量继续重复。

### 12.6 到时仍未稳定

达到 60 分钟硬上限后结束课程，根据失败位置自动安排：

- 规则未理解：8–10 分钟对比讲解回炉；
- 能识别但不会生成：12–15 分钟递减支架回炉；
- 句子正确但段落失效：15–20 分钟限时段落回炉；
- 连续两次延迟迁移失败：重新生成一节完整课程，并更换讲解视角和语境。

## 13. 临时通过与能力状态

### 13.1 本课结果

每个目标显示三种即时结果：

- **临时通过**：可以无提示生成并完成即时近迁移；
- **发展中**：在提示下可以完成，独立输出不稳定；
- **尚未稳定**：核心概念或综合应用仍失败。

识别题正确率只用于调整练习分支，不用于替代输出能力。核心目标临时通过必须同时满足：

- 两个语义或话题不同的句子首次、无提示正确；
- 一个综合任务对应的目标评分维度通过，且没有再次出现核心错误；
- 出门测使用未见过的表层形式，并在首次无提示作答时通过；
- 所有进入门槛的 AI 判断达到最低置信度；低置信结果必须用补测验证；
- 规划器在发布课程前已确认课程中存在取得全部必需证据的机会。

如果综合任务没有自然使用该能力的机会，结果记为“无证据”，既不判失败，也不能判临时通过。次要目标只需一次无提示生成和一次综合应用；旧目标仅产生复习证据，不在本课升级为 `retained` 或 `transferred`。

“能在自检阶段自己发现并修正相关问题”作为重要加权证据，但如果用户没有自然产生错误，不强制要求为了证明自检能力而故意出错。

### 13.2 能力等级与当前稳定性

能力记录拆成三个互不覆盖的字段：

- `highest_attained_level`：历史达到的最高证据等级；
- `current_stability`：`stable / unstable / needs_review`；
- `latest_lesson_outcome`：`applied / developing / not_stable`。

等级所需硬证据：

```text
diagnosed   作文中出现高置信问题
practicing  已进入专项训练；识别和受控修复最多推进到此
applied     无提示生成 + 本课即时近迁移
retained    跨时间、无目标提示的独立输出成功
transferred 不同题目中存在自然使用机会且无提示成功
```

证据权重只能用于排序和置信度，不能用许多低级识别题累加替代高级硬证据。后续复发不删除或降低 `highest_attained_level`，只改变 `current_stability` 并创建复习任务；已迁移的旧目标参加复习课后也不得降级。

### 13.3 自检证据等级

自检分别记录：

- `targeted_self_check`：系统明确提醒当前目标后发现；
- `general_self_check`：只给通用检查清单后发现；
- `spontaneous_detection`：没有目标提示时自主发现。

三类证据不能混为同一种“自检成功”。Version 2 自检前版本和自检后版本必须分别保存。

## 14. 暂停、中断与恢复

### 14.1 暂停点

- 每道题提交后都是自然暂停点；
- 作答中也可以立即暂停，草稿自动保存；
- 已完成模块永久保留；
- 退出不清零、不扣连续记录、不使用羞耻式文案。

暂停时主按钮只有“保存并退出”。系统根据时间自动判断稍后继续还是移到下一个学习时段，并同步调整建议重写窗口。“结束这节课程”放在次级菜单，明确不会删除已完成记录。

### 14.2 恢复规则

- 30 分钟内返回：直接回到原位置；
- 30 分钟至 24 小时：先做一道约 30 秒的重新激活题；
- 超过 24 小时：先完成约 2 分钟无提示重新基线，再继续；此前受控题仅作历史学习证据，最终 `applied` 主要依据恢复后的独立输出和近迁移；
- 超过 7 天：不强迫继续陈旧长任务，将剩余核心内容压缩成新的短复习。

恢复页示例：

> 上次你正在练习“自然表达学业压力”。先用 30 秒找回状态。

### 14.3 多次中断

如果同一专项课在 7 天内发生 2 次非正常中断，系统只把**剩余核心内容**自动拆成最长 20–25 分钟的模块；已完成内容永久保留，近迁移和出门测放在最后一个模块，不要求用户再次选择。

## 15. 专项训练完成页

完成页应在 30–60 秒内看懂。

第一屏显示：

- 实际训练时长；
- 核心目标的即时状态；
- 一句能力结论；
- 已自动安排的短复习和重写日期；
- 唯一主按钮“完成并返回今日计划”。

标题和总结必须根据课程结束原因变化：

- 达标：`本次专项训练已完成`；
- 到时未稳定：`本次训练时间已结束，巩固已安排`；
- 用户提前结束：`本次进度已保存`。

示例：

> 已完成 58 分钟训练  
> 核心目标“自然表达学业压力”：临时通过  
> 相关目标“补齐因果机制”：发展中  
> 你已能无提示使用 `face less academic pressure`；因果链仍需在重写时检查。  
> 本篇将在 8 月 15 日 20:00 解锁重写。

下方三张卡片：

1. **我现在能做到什么**；
2. **重写时仍需检查什么**；
3. **下一步已经怎样安排**。

详细错题、原句与最终改写、完整讲解放在可展开区域，不要求用户手动整理笔记或安排日期。

完成页同时显示只读时间线，例如：

> 今天到这里 → 明天 10 分钟短复习 → 短复习结束至少 24 小时后重写

能力结论必须逐目标生成，不能用一句笼统成功文案覆盖仍处于“发展中”的目标。

## 16. 专项课生成与 AI 边界

### 16.1 生成输入

课程生成器接收：

- 本次作文题目与用户原文；
- 2–3 个候选高优先问题；
- 每个问题的原文证据位置；
- 问题分类、严重程度和 AI 置信度；
- 用户历史同类问题；
- 用户目标分数和反馈语言；
- 最近一次练习、重写和迁移表现；
- 课程目标时长 60 分钟。

每个候选问题必须映射到有限的 `SkillDefinition`，不能每篇随意创造新的能力名称。

### 16.2 目标选择

候选问题按以下因素排序：

- 对 IELTS 分数的影响；
- 在本篇及历史作文中的复发频率；
- 能否迁移到大量题目；
- 诊断置信度；
- 是否适合通过一节课训练；
- 与其他目标是否高度相关。

最终形成：1 个必选核心目标、0–1 个符合加入条件的相关次要目标、0–1 个旧目标复习项。目标不足时不强行补齐。

### 16.3 确定性规划器负责

- 课程总时长和模块时间；
- 核心目标必须覆盖的认知阶段，以及次要 / 旧目标各自较轻的证据要求；
- 题型和数量上限；
- 自适应分支规则；
- 选择、排序、匹配等闭合题判分；
- 提示和重试记录；
- 能力状态更新；
- 重写日期和提醒；
- 幂等生成、版本控制和数据落库。

### 16.4 AI 负责

- 将用户原句填入受约束题型模板；
- 生成难度相近的新语境；
- 生成自然且不提前泄露答案的干扰项；
- 为开放改写生成可接受答案和量表；
- 判断多种合理句子；
- 评价桥梁句和微型段落；
- 给出针对本次答案的简短解释。

### 16.5 AI 不得自行决定

- 无限追加题目；
- 将一次课程结果直接标记为最终掌握；
- 随意跳过高优先问题；
- 重写日期；
- 能力状态转移规则；
- 用目标分数影响本次 IELTS 估分；
- 把“未采用推荐范例”自动判为错误。

### 16.6 生成校验

每道题发布前至少校验：

- 是否准确引用用户原句；
- 每个评分维度是否只对应一个训练目标；综合题可以包含多个独立评分维度；
- 是否有清楚答案或评分量表；
- 选择题是否存在明确最佳答案；
- 改写是否保持原意；
- 迁移题是否真的更换语境；
- 是否提前泄露后续主动输出答案；
- 语言是否自然；
- 全课预计时长是否在范围内。

每个目标预生成 1–2 道备用补救题，仅在分支触发时展示。

题目生命周期：

```text
DRAFT → VALIDATING → VALIDATED → PUBLISHED
                    ↘ REJECTED → REGENERATING
```

自动校验失败的题目不能展示给用户。MVP 优先使用人工验证过的稳定模板，由 AI 填入受约束内容；开放题判分另设人工标注回归集。

### 16.7 MVP 支持能力矩阵

MVP 先限定以下 13 个高频能力，未支持问题仍可进入批改报告，但不自动生成专门课程；系统回退到相邻的通用改写或论证课，并明确说明覆盖范围。

| IELTS 维度 | `skill_id` | 能力 | 主要题型 |
|---|---|---|---|
| GRA | `complete_comparison` | 比较对象和比较结构完整 | 最小对比、骨架、改写 |
| GRA | `verb_form_trigger` | 介词、不定式等触发的动词形式 | 定位、填空、造句 |
| GRA | `sentence_boundary` | 避免残句、粘连句和逗号拼接 | 定位、合并 / 拆分、段落自检 |
| GRA | `subject_verb_agreement` | 高频主谓一致 | 最小对比、修复、段落自检 |
| GRA | `article_control` | 冠词选择与可数性 | 最小对比、修复、段落自检 |
| LR | `collocation_perspective` | 自然搭配与英语信息组织视角 | 含义岔路、表达地图、一意多写 |
| LR | `word_form_precision` | 词性、词形和意义精确度 | 配对、最小对比、限定改写 |
| TR | `task_instruction_coverage` | 完整回应题型动作与题目范围 | 题型识别、thesis 对比、提纲 |
| TR | `mechanism_chain` | 补齐观点—原因—机制—结果 | 角色卡、因果链、桥梁句 |
| TR | `development_relevance` | 解释、例证与核心观点相关 | 相关性筛选、删除、微型段落 |
| TR | `weighing_qualification` | outweigh、让步和比较标准 | 权衡选择、限制条件、段落写作 |
| CC | `paragraph_function_order` | 句子功能与逻辑顺序 | 标注、排序、反向提纲 |
| CC | `reference_linking` | 指代清晰和逻辑连接准确 | 指代修复、连接关系辨析、重建 |

每个 `skill_id` 必须拥有：允许题型、固定成功门槛、可接受答案规则、最低判分置信度和回退策略。

## 17. 核心数据模型

### 17.1 `TrainingCycle`

一轮完整训练的聚合实体：

- 用户；
- 题目；
- 核心周期阶段（到 Version 2 对比完成为止）；
- Version 1；
- 批改报告；
- 专项课程；
- 重写任务；
- Version 2；
- 版本对比；
- 关联的 `TransferTask`；
- 创建、更新时间和核心周期完成时间。

Version 2 比较完成代表核心训练周期完成，但相应能力目标仍可保持开放，直到迁移检测取得证据。不能用一个线性字段同时承载课程、重写和迁移三个可并行任务的状态。

### 17.2 `SkillDefinition`

- `id`；
- 名称；
- IELTS 维度：TR / CC / LR / GRA；
- 父级能力；
- 规则和描述；
- 支持的题型模板；
- 前置能力；
- 版本。

### 17.3 `IssueEvidence`

- `essay_attempt_id`；
- `skill_id`；
- 原文段落与字符范围；
- 原句快照；
- 问题解释；
- 严重程度；
- 诊断置信度；
- 用户是否质疑或修正。

### 17.4 `LearningObjective`

- `training_cycle_id`；
- `source_evidence_ids[]`，允许引用当前或历史作文；
- `skill_id`；
- 类型：核心 / 次要 / 旧目标复习；
- 优先级；
- 本课成功标准；
- 课前状态；
- 课后结果。

### 17.5 `LessonPlan` 与 `LessonBlock`

`LessonPlan`：

- 对应训练轮次；
- `planned_user_seconds`、`lesson_clock_seconds`、`productive_seconds`、`paused_seconds` 和 `ai_wait_seconds`；
- 状态：`PLANNING / READY / ACTIVE / CORE_COMPLETED / TIMEBOX_EXPIRED / USER_SKIPPED / ABANDONED`；
- `completion_reason`；
- 目标列表；
- 预计题量和时长；
- 规划器版本；
- AI 生成版本；
- 创建、开始、完成时间。

`LessonBlock`：

- 对应目标或综合模块；
- 顺序和时间预算；
- 当前分支；
- 完成状态；
- 恢复位置。

### 17.6 `ExerciseItem`

- `block_id`；
- 必填 `learning_objective_id` 与 `primary_skill_id`；
- 可空 `source_issue_id`，迁移题和历史复习题不强制对应当前作文句子；
- 阶段：notice / understand / control / produce / near_transfer / self_check；
- 题型；
- 题目与材料；
- 答案规范或评分量表；
- 提示层级；
- 难度与预计用时；
- 是否备用题；
- 生成方式：模板 / AI；
- 质量校验状态。

综合题额外包含 `ExerciseCriterion[]`，每项独立保存：

- `objective_id`；
- `skill_id`；
- 评分量表和通过标准；
- 得分、证据和置信度。

每个 criterion 分别生成能力证据事件，避免一个综合段落只能归到一个技能。

### 17.7 `ExerciseResponse`、`ExerciseAttempt` 与 `ExerciseEvaluation`

`ExerciseResponse` 保存首答、终答和当前有效评估的汇总索引。

每次提交生成独立 `ExerciseAttempt`：

- 答案和提交时间；
- 本次用时与自信度；
- 提交时的提示快照和提示层级；
- 是否已经查看参考答案。

每次判分生成独立 `ExerciseEvaluation`：

- 各维度得分与用户答案证据；
- 即时反馈；
- 判分置信度；
- 判分器、提示词和量表版本；
- `supersedes_evaluation_id`；
- `adjudication_status`。

重新评估不能覆盖旧结果，只能通过 supersede 关系指定当前有效评估。

### 17.8 `SkillEvidenceEvent` 与 `UserSkillState`

每次首写、练习、重写或迁移都形成一条能力证据事件，记录：

- 用户和能力；
- 来源；
- 难度；
- 是否独立完成；
- 得分；
- 证据权重；
- `objective_id` 和 `evaluation_id`；
- `confidence`；
- `valid_for_state_transition`；
- `adjudication_status`；
- `source_entity_type / source_entity_id`；
- 时间。

`UserSkillState` 保存：

- `highest_attained_level`；
- `current_stability`；
- `latest_lesson_outcome`；
- 最近证据；
- 最近练习时间；
- 复发次数；
- 连续独立成功次数；
- 下次复习时间。

建议证据权重：

| 证据 | 权重 |
|---|---:|
| 识别题 | 1 |
| 受控修正 | 2 |
| 主动生成 | 3 |
| 即时近迁移 | 4 |
| 综合段落 | 4 |
| 延迟重写 | 5 |
| 后续陌生题独立应用 | 5 |

权重只用于证据排序和置信度展示，能力状态变化仍必须满足第 13.2 节的硬证据类型。

### 17.9 `RewriteTask` 与 `TransferTask`

`RewriteTask`：

- `target_rewrite_at`：专项课生成时的建议窗口；
- `due_at`：核心路径或短回炉结束后计算的正式解锁时间；
- `last_instruction_exposure_at`；
- 状态：`PLANNED / LOCKED / READY / ACTIVE / COMPLETED / SKIPPED_PREREQUISITE / RESCHEDULED`；
- Version 2 自检前和自检后快照。

`TransferTask`：

- 对应目标与自然使用机会定义；
- `due_at`；
- 状态：`PLANNED / READY / COMPLETED / NO_OPPORTUNITY / RESCHEDULED`；
- 是否无提示；
- 迁移证据结果。

### 17.10 幂等与唯一性

- 课程生成、单题生成、答案提交、AI 判分和能力证据写入均使用独立幂等键；
- `ExerciseAttempt` 的客户端提交 ID 建立唯一约束；
- `ExerciseEvaluation` 通过 attempt、判分器版本和重评序号去重；
- `SkillEvidenceEvent` 通过来源实体、目标和证据类型去重；
- AI 回调乱序时只更新指向当前有效评估的索引，不覆盖更晚结果。

### 17.11 `ProviderConnection`、`ModelRoute` 与 `AIJob`

`ProviderConnection` 只保存连接元数据和 secret 引用：

- 作用域：实例级 / 当前后端会话；
- 显示名称、adapter 类型和规范化 base URL；
- 凭据来源：环境变量 / 会话内存 / 后端加密存储；
- `secret_ref`、加密版本和可选末四位，永不在业务表中保存明文；
- 允许的自定义 header 名称及其独立 secret 引用；
- 连接状态：`UNCONFIGURED / TESTING / READY / COMPATIBLE / INVALID / DISABLED`；
- 能力快照：结构化输出、上下文长度、流式输出和模型列表能力；
- 教学质量状态：`CALIBRATED / COMMUNITY_VALIDATED / UNVERIFIED`；
- `config_version`、最近测试时间、测试结果摘要和创建 / 修改人。

`ModelRoute` 使用不可变版本记录每类任务的实际路由：

- 任务类型：assessment / generation / evaluation / fallback；
- `provider_connection_id`、连接配置版本和 model ID；
- 受支持参数的白名单与结构化输出策略；
- fallback 是否启用、触发条件和是否允许跨 provider；
- 校准集、prompt、rubric 和 route 版本；
- 生效时间、停用时间和修改人。

修改配置时创建新版本，已经入队或运行中的任务继续使用创建时的路由快照；新任务才使用新版本。不得因为管理员在分析途中切换模型而让同一任务混用多个配置。

`AIJob` 保存可恢复调用状态，但默认不保存完整 prompt 或作文副本：

- 用户、训练轮次、来源实体和任务类型；
- 内容发送授权：待确认 / 已授权自动发送 / 仅手动提交；
- 状态：`WAITING_FOR_CONSENT / QUEUED / LEASED / RUNNING / RETRY_SCHEDULED / AI_BLOCKED / SUCCEEDED / FAILED / CANCELED`；
- provider、connection、route、model、prompt、schema 和 rubric 的版本快照；
- 幂等键、尝试次数、lease、下次重试时间和最后错误分类；
- provider request ID、延迟、token usage 和可信时才记录的估算成本；
- 创建、开始、完成和取消时间。

任务记录通过来源实体引用受保护的作文内容。诊断导出默认只包含非敏感元数据，不包含正文、完整 prompt、provider 响应或 secret。

## 18. 系统状态机

状态拆为多个实体，避免课程未完成、重写窗口和迁移任务互相矛盾。

### 18.1 核心训练周期

下图表示正常主路径；跳过专项课、时间到和改期通过各子任务状态表达。

```text
QUESTION_READY
    ↓
ATTEMPT_1_ACTIVE
    ↓
SUBMITTED
    ↓
ANALYZING
    ↓
FEEDBACK_READY
    ↓
LESSON_GENERATING
    ↓
LESSON_READY
    ↓
LESSON_ACTIVE
    ↓
LESSON_RESOLVED
    ↓
REWRITE_LOCKED
    ↓
REWRITE_READY
    ↓
ATTEMPT_2_ACTIVE
    ↓
COMPARING
    ↓
CORE_CYCLE_COMPLETED
```

`LESSON_RESOLVED` 可以来自核心路径完成、时间到、用户跳过或课程放弃；具体结果读取 `LessonPlan.status`。Version 2 对比完成后，核心周期完成，迁移检测作为关联的后续复习任务继续存在。

### 18.2 子任务状态

`LessonPlan.status`：

```text
PLANNING → READY → ACTIVE
                    ├→ CORE_COMPLETED
                    ├→ TIMEBOX_EXPIRED
                    ├→ USER_SKIPPED
                    └→ ABANDONED
```

`RewriteTask.status`：

```text
PLANNED → LOCKED → READY → ACTIVE → COMPLETED
             └→ RESCHEDULED
PLANNED / LOCKED → SKIPPED_PREREQUISITE → ACTIVE → COMPLETED
```

`TransferTask.status`：

```text
PLANNED → READY → COMPLETED
             ├→ NO_OPPORTUNITY → RESCHEDULED
             └→ RESCHEDULED
```

`AIJob.status`：

```text
WAITING_FOR_CONSENT → QUEUED → LEASED → RUNNING → SUCCEEDED
                         ↑         │         ├→ RETRY_SCHEDULED
                         │         │         ├→ AI_BLOCKED
                         │         │         └→ FAILED
                         └─────────┴─────────── 修复配置或到达重试时间后重新入队

WAITING_FOR_CONSENT / QUEUED / AI_BLOCKED → CANCELED
```

`AI_BLOCKED` 表示继续自动重试不会成功，例如 key 失效、余额不足、模型被删除或能力检测不满足；它必须向管理员显示可执行修复动作。修复后沿用原幂等键重新入队，不能创建第二份批改。短暂限流或网络超时进入 `RETRY_SCHEDULED`，不得无限占用运行状态。

网络中断、AI 失败、通知失败和被打断均作为可恢复标记，不把整个训练轮次推入不可恢复终止状态。

### 18.3 教学阶段

专项课生命周期不再用不可逆的教学阶段状态机。当前教学位置由 `active_block_id`、`active_item_id` 和 block 类型推导，并映射为五个用户可见阶段：诊断、理解、独立输出、应用、收尾。这样才能支持跳过讲解、回到支架、恢复激活题和交错复习。

## 19. 页面与信息架构

MVP 主要页面：

- **首次启动向导**：实例归属、使用模式和 AI 连接；
- **今天**：唯一下一步和预计用时；
- **写作室**：题目、编辑器、计时和字数；
- **反馈摘要**：估分、证据和核心目标；
- **专项训练**：单卡任务、阶段进度和安全暂停；
- **重写与对比**：闭卷 Version 2 和前后变化；
- **成长记录**：能力状态、高频复发和迁移记录。
- **设置 → AI 服务**：连接状态、密钥替换、模型路由、用量和故障；
- **系统状态**：版本、迁移、任务队列、备份和升级提示，仅管理员可见。

专项训练入口文案：

> 本篇专项训练  
> 预计 45–60 分钟 · 1 个核心目标 · 可能包含关联巩固  
> 先用 3 分钟完成第一道热身

中断后入口自动变为：

> 继续专项训练 · 从上次位置开始 · 预计剩余 24 分钟

## 20. 提醒与排程

推荐默认节奏：

- D0：Version 1、批改；
- D0–D1：最长 60 分钟专项训练；
- 最后一次实质教学暴露后 24–48 小时：Version 2；
- D5–D7：相邻陌生题的无提示迁移检测；
- D14：在新作文中混合检测旧目标。

只在关键异步节点通知：

- 批改和专项课已就绪；
- 专项课未完成且原计划重写窗口临近；
- 重写已解锁；
- 一次温和逾期提醒；
- 短回炉到期。

通知提供“开始”和“推迟一天”，遵守免打扰时段，不设置羞耻式 streak 惩罚。

## 21. 关键异常流程

### 21.1 AI 生成失败

- 自动重试失败的单题或单模块；
- 不重新生成已经完成的课程部分；
- 明确告诉用户作文和进度已保存；
- 多次失败后允许手动重新生成；
- 不生成空白课程或错误解锁重写。

### 21.2 开放题判分低置信度

- 使用中性反馈；
- 不阻塞课程；
- 允许用户请求重新评估；
- 将该证据标记为 `valid_for_state_transition = false`；
- 自动使用补测题取得新证据；
- 不凭一次低置信判断改变能力状态。

### 21.3 现实生活中断

- 自动保存到当前题和光标位置；
- 只计算实际训练进度；
- 剩余内容自动成为首页下一步；
- 多次中断后自动拆分课程。

### 21.4 专项课未完成但原计划重写窗口已错过

首页优先提示完成剩余核心路径，并显示预计时间。此时正式 `due_at` 尚未生成，不能显示“重写逾期”。用户仍可跳过并开始重写，但重写任务标记为 `SKIPPED_PREREQUISITE`，比较报告说明条件差异，结果不能升级为 `retained`。

### 21.5 短回炉与重写冲突

短回炉包含实质讲解、完整答案或支架时，系统更新 `last_instruction_exposure_at`，并将 Version 2 自动顺延到至少 24 小时后。用户坚持立即重写时，标记为“复习后应用”，不能作为保持证据。

### 21.6 积压过多

- 同时最多保留两轮活动训练；
- 优先安排旧作文重写和短回炉；
- 不持续推送新题制造积压。

## 22. 产品与学习指标

### 22.1 低摩擦指标

- 首页到第一道专项题不超过一次点击；
- 进入后 5 秒内可以开始作答；
- 任意时刻退出不丢失输入和分支；
- 恢复训练不超过一次点击；
- 用户无需手动整理错误或安排重写日期；
- 完成页不要求额外管理决策。

### 22.2 学习效果指标

- 首次独立作答正确率；
- 提示依赖率；
- 主动生成通过率；
- 本课即时近迁移通过率；
- Version 2 旧错误复发率；
- 定向自检、通用自检和自主发现率；
- 论证链完整度；
- D5–D7 迁移成功率；
- 每百词同类错误频率变化；
- 7 天内“首写—专项课—延迟重写—迁移检测”完整闭环完成率；如 D5–D7 任务因没有自然使用机会而改期，则单独记录，不把用户判为未完成。

不以“生成了多少份报告”“做了多少道题”作为核心价值指标。

## 23. MVP 验收标准

1. 输入一篇作文和候选问题后，可自动形成 1 个核心目标、0–1 个符合条件的次要目标和 0–1 个旧目标；首次用户或只有一个高置信问题时仍能生成合法课程。
2. 系统能生成预计 45–60 分钟、上限 60 分钟的专项课。
3. 核心路径至少包含前测、一次受控修正、两个不同语境的无提示生成、一次综合应用和出门测；课程发布前能证明全部成功门槛可取得证据。
4. 按 `expected_active_seconds` 计算，核心路径主动输出不少于 65%；识别、选择和判断最多 3–4 道且不超过任务量的 25%。
5. 所有题目可以追溯到 `learning_objective_id`、`skill_id` 及目标的当前或历史诊断来源；近迁移题允许没有当前作文的 `source_issue_id`。
6. 含义岔路能影响后续表达路径，且不计入正确率。
7. 闭合题拥有明确允许答案，同样回答重复判分结果一致。
8. 开放题返回分维度得分、用户答案证据、最重要建议和置信度；综合段落能向多个目标分别写入 criterion 和证据。
9. 用户使用提示后答对不会覆盖首次错误记录。
10. 连续错误能触发一层补救分支；连续独立正确能跳过重复练习。
11. 每个目标最多追加两题，课程不会无限延长。
12. 段落实验室只围绕当前目标反馈，不把所有新错误同时展开；独立生成组在 2–3 个答案全部提交后再统一反馈。
13. 用户至少完成一次定向自检和二次修改。
14. 课程最多产生 `applied` 级新证据，且不会降低旧目标的 `highest_attained_level`；复发只改变稳定性。
15. Version 2 前 35 分钟不展示目标，最后 5 分钟才展示抽象自检目标；系统分别保存自检前后版本，且不泄露原句答案和完整范文。
16. 用户中途关闭页面后，可以恢复到原题、草稿、提示层级和当前分支。
17. 多次中断后，系统能够自动拆分剩余课程而不清零进度。
18. AI 生成或判分失败时可以重试单题，不要求重新生成整节课。
19. 专项课达到 60 分钟时不会丢失当前输入，并能以 `TIMEBOX_EXPIRED` 结束、裁剪可选内容和自动安排对应短回炉。
20. `target_rewrite_at` 与正式 `due_at` 分离；短回炉会将重写顺延到最后一次教学暴露至少 24 小时后；跳过专项课的重写不会产生保持证据。
21. 核心周期、课程、重写和迁移任务使用独立状态；专项课未完成时不会错误显示“重写已到期”。
22. D5–D7 迁移检测必须自然提供使用目标能力的机会但不提示规则；没有机会时记录 `NO_OPPORTUNITY`，不用“不使用”判失败。
23. 低置信或争议中的 AI 判分不能改变能力状态，补测成功后才能生成有效状态证据。
24. AI 单题重试、重复提交和并发回调不会创建重复题目、重复判分或重复能力证据。
25. 人工抽检生成题时，目标清楚、答案明确、原意准确、语言自然的合格率达到 95% 以上；开放题判分另有独立标注回归集。

## 24. 本轮已确认的产品决策

- 项目从首个公开版本起使用 OSI 认可许可证并支持完整自托管，核心能力不得依赖维护者的闭源服务；
- 管理员可在首次启动向导和设置页连接自己的 AI 服务，浏览器不得直接持有 key 调用 provider；
- 本地优先提供 Docker Compose 一条命令启动，云端至少维护一个真正经过 E2E 的部署模板；
- 最终发布必须同时交付独立 `coach-ielts-writing` Codex Skill，服务不想使用网页的用户；
- Web 与 Skill 共用版本化教学协议、能力 ID 和学习档案交换格式；
- 专项练习从 10–15 分钟升级为最长 60 分钟能力课，由 35–45 分钟核心路径和最多 15 分钟弹性路径组成；
- 60 分钟用于完整能力转化，不用于机械增加题量；
- 每课必须有 1 个核心问题；相关新问题和旧目标各为 0–1 个且必须满足加入条件；
- 中式英语训练必须先确认真实意思，再选择英语表达视角；
- 选择题只用于诊断和辨析，主要训练形式是改写、生成、论证链、微型段落和自检；
- 首次独立答案、提示后答案和最终答案分开记录；
- 完成专项课不等于掌握；本课只能证明即时应用，延迟无提示输出和不同题目迁移才是后续验证；
- 系统自动排程、提醒、恢复和回炉，用户不承担训练管理工作；
- 完整范文在 Version 2 提交前锁定；
- 所有课程必须可暂停、可恢复、可动态裁剪，且不得无限延长；
- 重写正式解锁时间从最后一次实质教学暴露开始计算；短回炉会自动顺延重写。

## 25. 开源与可自托管要求

### 25.1 许可证与开放范围

- 默认采用 Apache License 2.0；在第一次公开 release 前可以由项目所有者改为另一项 OSI 认可许可证，但不得使用仅“source available”的非开源许可证；
- 前端、后端、异步任务、数据库迁移、AI provider adapters、提示词模板、评分结构、专项课规划器和 Codex Skill 全部在同一公开仓库；
- 核心训练闭环不得依赖项目维护者控制的闭源 API、私有数据库或付费 SaaS；
- 可以未来提供便利性的官方托管版本，但自托管版必须拥有同等核心学习能力；
- 项目名称和视觉标识可以另行声明商标规则，但商标规则不得妨碍代码自托管。

许可证决策点：默认 Apache-2.0 适合鼓励个人、学校和企业广泛部署与二次集成；如果项目所有者希望任何对公众提供服务的修改版也必须公开对应源码，应在首次 release 前评估 AGPL-3.0。公开发布后不应随意回溯改变已有版本的许可证。

### 25.2 仓库最低文件

公开仓库至少包含：

- `LICENSE`；
- 根目录 `README.md`，包含本地运行、部署按钮、升级和 Skill 安装入口；
- `CONTRIBUTING.md`；
- `SECURITY.md`，包含私密漏洞报告方式和支持版本；
- `CODE_OF_CONDUCT.md`；
- 架构决策记录与数据迁移说明；
- `.env.example`，只能包含变量名和安全示例，不能包含真实密钥；
- 锁定依赖文件、数据库迁移、示例数据和自动化测试；
- `.agents/skills/coach-ielts-writing/`；
- `deploy/` 下的本地与云端部署模板。

### 25.3 开源可运行性

- 全新机器只凭公开仓库和公开依赖即可构建；
- 无 AI key 时也能启动 UI、完成账户初始化、浏览示例课程并运行确定性测试；
- 提供不会产生真实模型费用的 fixture / mock provider；
- 发布可复现的版本化容器镜像，并提供 `linux/amd64` 与 `linux/arm64` 架构；
- 数据库 schema、提示词版本、能力分类和 Skill 数据交换格式均有显式版本；
- 禁止将必要的构建产物、密钥或私有 npm / package registry 作为启动前提。

### 25.4 隐私、遥测与供应链

- 默认不启用产品遥测；如果未来增加匿名遥测，必须在 UI 中明确说明并由管理员主动选择开启；
- 作文内容、API key、模型完整请求与响应默认不进入应用日志；
- 导出、诊断包和错误报告默认排除 secret 与作文正文，用户主动选择后才能包含正文；
- CI 运行单元测试、集成测试、依赖漏洞扫描、许可证检查、secret scanning 和容器构建；
- 每个 release 生成 SBOM、校验和、迁移说明和已知破坏性变更说明；
- 依赖升级与 prompt / rubric 变更必须通过回归样本，不能只验证代码能够编译。

### 25.5 数据所有权与可退出性

- 用户拥有作文、反馈、训练回答和能力档案；
- 支持完整 JSON 导出、可读 Markdown 导出和彻底删除；
- 导出格式不得包含 provider secret；
- Web 导出必须能够被随项目发布的 Codex Skill 导入；
- Skill 产生的兼容学习档案也应能够重新导入 Web；
- 用户停止使用某一 AI provider 后，历史记录仍可读取和导出。

## 26. UI 配置自有 AI（BYOAI）

本项目把用户自带模型服务称为 BYOAI（Bring Your Own AI），避免与云厂商“自带加密密钥”的 BYOK 概念混淆。

### 26.1 MVP provider 范围

首个公开版本必须支持：

1. **OpenAI**：官方 API endpoint；
2. **OpenAI-compatible**：用户自定义 HTTPS / HTTP base URL、model ID 和可选 API key，覆盖常见代理网关与本地推理服务；
3. **Mock provider**：只用于演示、开发和端到端测试，不输出真实 IELTS 估分。

provider registry 必须可扩展。Anthropic、Gemini、Azure OpenAI 及其他原生 adapter 可在后续版本加入，但不得把它们写死在学习业务层。

### 26.2 首次启动向导

首次打开显示“IELTS Writing 已启动，还差一步即可使用 AI 批改”，并提供“连接 AI 服务”“使用预生成示例体验”和“暂不配置”。如果部署环境已经提供完整连接配置，系统自动测试，通过后直接进入首页，失败时才进入修复向导。

首次启动只要求完成必要选择：

1. 选择单用户或多用户部署模式；
2. 创建首位管理员；
3. 选择“立即配置 AI”或“稍后配置”；
4. 选择 provider；
5. 输入 API key 和 model ID；仅选择 compatible endpoint 时再输入 base URL 和鉴权方式；
6. 选择“仅本次会话”或“加密保存”；
7. 执行连接测试与能力检测；
8. 使用默认单模型路由，或进入高级设置；
9. 完成后直接进入示例课程或今日任务。

个人模式中当前用户同时是管理员和学习者；共享模式默认关闭公开注册，只有管理员可以配置 AI 服务。公网或非本机首次初始化必须验证部署时生成的一次性 setup token，不能让“第一个访问者”自动取得管理员权限。

除管理员、安全模式和 provider 外，其他字段均提供可靠默认值。默认向导最多要求 provider、API key 和 model 三项；只有选择 compatible endpoint 时才展示 base URL。用户不需要先理解 temperature、JSON Schema、token limit 等模型参数。

### 26.3 配置界面

每个 `ProviderConnection` 显示：

- 显示名称；
- provider 类型；
- base URL；
- API key 输入框；
- 可选 organization / project / 自定义 header；
- model ID；
- secret 保存模式；
- 最近连接测试时间；
- 连接状态；
- 能力校验状态；
- “测试连接”“设为默认”“删除密钥”操作。

保存后只能显示密钥是否存在和末四位，不能重新显示完整明文。修改密钥必须重新输入。

默认主操作为“测试并保存”：系统先用固定无隐私样本完成临时测试，成功后才写入正式配置。失败时保留表单输入但不保存无效连接。连接测试不得使用真实作文，也不得进入学习历史。

### 26.4 简单与高级模型路由

默认模式使用一个模型完成所有开放 AI 任务。高级模式允许分别选择：

- `assessment_model`：四项估分、证据和版本比较；
- `generation_model`：讲解、题目和参考表达生成；
- `evaluation_model`：开放句子、桥梁句和段落量表判分；
- `fallback_model`：主路由超时或 provider 不可用时的可选后备。

确定性匹配、排序和固定答案判分永远不调用模型。用户可以一键恢复为单模型默认设置。

### 26.5 连接测试不等于教学质量通过

系统必须分别展示：

- **连接正常**：认证、endpoint 和基本生成可用；
- **结构化输出可用**：能否稳定返回系统要求的 schema；
- **上下文容量满足**：可以处理作文、评分规则和反馈上下文；
- **教学质量状态**：已校准 / 社区验证 / 未验证。

未知模型即使连接成功，也必须标记“未校准”。用户可以继续使用，但 UI 明确说明 IELTS 估分和开放题判分可能不稳定。模型升级或 model ID 改变后重新运行最小回归测试。

测试结果分为“可正常使用”“兼容模式可用”和“不满足要求”。例如不支持原生 JSON Schema 时可以进入兼容提取与重试流程；认证失败、模型不存在或结构持续失败时不能设为默认评分模型。技术详情可以显示状态码、request ID 和延迟，但不得包含 header、key 或完整敏感响应。

### 26.6 密钥安全

- 浏览器只负责通过 TLS 把密钥提交给本实例后端；浏览器不得直接携带密钥请求 AI provider；
- 支持“仅会话使用”：密钥只存在于后端会话内存，过期、退出或服务器重启后清除；
- 支持“加密保存”：使用每条 secret 独立 nonce 的认证加密，主加密密钥来自实例环境或持久化 secret store；
- 数据库只保存密文、nonce、版本、provider 和末四位；
- secret 绝不写入普通日志、错误堆栈、分析事件、导出文件、浏览器 local storage 或前端状态持久层；
- 所有日志对 `Authorization`、API key、自定义敏感 header 和常见 key 格式进行集中脱敏；
- 删除连接时立即删除密文；轮换主加密密钥时支持逐条重加密；
- 环境变量提供的实例级 key 在 UI 中显示为“由部署环境管理”，不能查看或导出；
- MVP 多用户模式只支持管理员配置实例级共享连接；普通用户不能查看 provider 技术详情、密钥状态或修改路由；
- 每用户自带独立 key 作为后续版本，避免 MVP 同时承担个人密钥隔离、额度和复杂结算；
- 用户配置的 custom base URL 必须经过 SSRF 防护、重定向复检和超时限制；公网多用户模式默认禁止访问私有 / 保留网段，管理员可以针对自托管内网显式放行。

“仅会话使用”只面向个人模式或当前管理员会话。依赖该连接的后台任务在会话过期或服务重启后必须进入 `AI_BLOCKED` 并提示重新提供 key，不能假装仍可自动恢复。共享实例若需要无人值守后台批改，必须使用环境变量或加密保存模式。

### 26.7 Provider adapter 契约

学习业务层只调用统一接口：

```text
testConnection(connection)
probeCapabilities(connection, model)
generateStructured(task, schema, context)
generateText(task, context)
normalizeUsage(response)
classifyError(error)
```

adapter 负责认证、endpoint 差异、结构化输出、重试 header 和 usage 归一化；学习引擎只处理版本化的内部 schema。新增 provider 不得修改评分、专项课或能力状态逻辑。

### 26.8 请求记录、成本与隐私

- 默认记录 task 类型、provider、model、延迟、token usage、结果状态、请求 ID 和版本，不记录作文或完整 prompt；
- 如果 provider 返回 usage，展示本次和本轮 token 用量；
- 只有存在可信价格配置时才显示成本估算，并明确标记“估算”；
- 支持每轮、每日和每月软预算；达到阈值前提示，未经用户允许不自动切换到另一家 provider；
- UI 明确显示“本次作文将发送到：provider / base URL”；
- 项目服务器以外，内容只会发送到用户选定的 provider；
- 超时、限流、余额不足、模型不存在、schema 不兼容和内容过长使用不同的可执行错误文案。

### 26.9 无 AI 与故障降级

- 没有配置 AI 时，写作室、历史记录、导入导出和确定性示例课程仍可使用；
- 需要 AI 的任务进入可恢复队列，不能丢失作文；
- provider 故障时允许稍后重试、选择其他已配置连接或导出给 Codex Skill 继续；
- fallback route 默认关闭，用户开启后才允许跨模型自动重试；
- mock provider 的结果必须带明显演示标记，不能混入真实能力趋势。

首稿提交时，如果 AI 当前不可用，用户选择“保存并等待 AI 恢复”（默认）或“仅保存，不自动发送”。AI 恢复后，只有前者自动进入分析队列，避免违背数据发送意图。

### 26.10 单用户与共享模式

个人模式：

- 当前用户同时是管理员和学习者；
- 隐藏团队、邀请和复杂权限设置；
- 支持在 UI 配置 key，也支持由环境变量管理；
- 只监听本机时允许简化登录；一旦监听局域网或公网，必须启用认证。

共享模式：

- 默认关闭公开注册；
- 只有实例管理员可以配置 provider、base URL、key、模型路由和预算；
- 普通用户的作文、反馈、练习和能力档案相互隔离；
- 管理员默认只能查看运行状态和聚合用量，不能自动读取用户作文正文；排障访问正文需要单独授权和审计；
- provider 配置、密钥替换、路由和额度变更写入不含 secret 的审计日志；
- 从个人模式切换为共享模式前，必须完成认证、注册策略、数据访问策略、额度策略和隐私告知，不能只切换一个开关。

### 26.11 BYOAI 验收标准

1. 没有环境变量的新实例可以在 2 分钟内完成一次 OpenAI 连接配置；有完整环境变量时无需重复填写，只进行自动验证。
2. 默认向导最多要求 provider、key 和 model 三项；只有 compatible endpoint 才要求 base URL。
3. “测试并保存”具有原子性：测试失败不落库为可用配置，也不清空用户刚填写的表单。
4. 测试只使用固定无隐私样本，不消耗或发送任何真实作文。
5. 供应商不支持列举模型时，管理员仍可手工输入 model ID；不支持原生 JSON Schema 时可以在明确标注的兼容模式中工作。
6. 原始 key 无法从浏览器接口、页面源码、日志、错误追踪、备份、导出或普通数据库字段中恢复。
7. 环境变量管理的 key 只能测试，不能在 UI 查看、覆盖或删除；UI 保存的 key 可以安全替换和删除。
8. 普通用户无法读取或修改 provider、key、模型路由和技术测试结果。
9. AI 未配置或故障时，写作、自动保存、历史查看和数据导出仍然可用。
10. 连续不可恢复错误进入 `AI_BLOCKED`；管理员修复连接后，等待任务能批量、幂等恢复且不重复收费。
11. 简单模式不要求用户理解多模型路由；高级路由默认折叠并可一键还原。
12. 每个 AI 任务都能追溯实际 provider、model、配置版本、token 和重试次数，但不会泄露作文正文或 secret。
13. 替换或删除 key 后，新任务不再使用旧 key；运行中的任务按其配置快照安全完成或明确失败。
14. 自定义 endpoint 的 SSRF、重定向绕过、DNS rebinding、超时和私网访问规则通过安全回归测试。

## 27. 一键部署与运行

### 27.1 MVP 运行架构

```text
浏览器
  ↓ HTTPS / 本机 HTTP
应用容器（Web UI + API + 持久化任务执行器）
  ├── PostgreSQL（业务数据、任务队列、审计事件）
  ├── 持久化数据卷（导入、导出、备份临时文件）
  └── 用户选定的 AI provider
```

- 采用模块化单体；MVP 不拆微服务；
- HTTP 服务和后台任务可以运行在同一镜像、不同进程中；默认小规模部署允许同一容器托管；
- AI 任务使用 PostgreSQL 持久化队列、lease、重试和幂等键，应用重启后能够恢复；
- MVP 不强制 Redis、Kafka、对象存储或外部任务 SaaS；
- 所有数据库迁移跟随应用版本发布，并在 readiness 通过前完成；
- 以后扩展独立 worker 时不得改变业务 API 和任务 schema。

### 27.2 官方支持的部署路径

| 方式 | 面向用户 | 发布要求 | 支持等级 |
|---|---|---|---|
| Docker Compose | 个人电脑、NAS、单机服务器 | `docker compose up -d` 启动应用与 PostgreSQL | Tier 1，必须通过每个 release 的 E2E |
| Railway Template | 不想管理服务器的个人用户 | 仓库提供 `Deploy on Railway`，自动创建应用、PostgreSQL 和随机 secret | Tier 1，首个公开 release 必须提供 |
| Render Blueprint | 希望使用基础设施声明文件的用户 | 仓库提供 `render.yaml`，创建 Docker 服务和 PostgreSQL | Tier 2，发布前验证或明确标记社区支持 |
| OCI 镜像 | 其他云、PaaS、Kubernetes 或家庭服务器 | GHCR 发布版本化 multi-arch image | Tier 1，提供通用环境变量契约 |

“一键部署”指用户点击部署按钮并确认账户 / 资源，或在安装 Docker 后运行一条命令；不能承诺免除第三方平台账号、账单确认或域名配置。

### 27.3 Docker Compose 本地体验

- 根目录提供 `compose.yaml`，可直接拉取发布镜像，也允许从源码构建；
- 默认只绑定 `127.0.0.1`，避免个人实例无意暴露到局域网；
- 应用与 PostgreSQL 使用命名数据卷；停止或更新容器不得删除数据；
- `docker compose up -d` 后由幂等 bootstrap 过程在专用持久卷生成实例 secret，设置最小文件权限，等待数据库健康、运行迁移并启动应用；已存在的 secret 永不因普通重启或升级而重新生成；
- 首次打开进入第 26.2 节向导，不要求用户先编辑 AI key 环境变量；
- `docker compose ps` 和 UI 系统状态页能明确显示数据库、任务执行器和版本状态；
- 提供受支持的 `backup`、`restore`、`doctor` 和 `upgrade` 命令包装，但基础启动不依赖 shell 脚本；
- 本地模型文档说明容器内 `localhost` 与宿主机的差异，并提供跨平台可验证的宿主机访问方案。

### 27.4 云端模板

云端模板必须：

- 从公开 GitHub 仓库或公开 GHCR 镜像部署；
- 创建应用和 PostgreSQL，并通过内部连接串关联；
- 自动生成 `SESSION_SECRET`、`MASTER_ENCRYPTION_KEY` 和一次性 `SETUP_TOKEN`；
- 不要求在部署页面填写 AI API key，AI 连接在首次 UI 向导中完成；
- 自动配置 HTTPS、公开 URL、健康检查和重启策略；
- 数据库默认不对公网开放，或使用平台允许的最严格入站规则；
- 显示预计产生的平台资源费用，不把第三方免费额度视为永久保证；
- 部署按钮指向固定 release 或受控稳定分支，不能静默跟随未经验证的提交；
- 模板变化与应用 release 一起做冒烟测试。

### 27.5 配置与 secret 优先级

部署环境只负责实例级基础 secret，AI key 可以随后在 UI 配置。配置优先级：

1. 只读部署环境变量；
2. 管理员在 UI 保存的加密配置；
3. 产品安全默认值。

环境变量提供的字段在 UI 标记“由部署环境管理”，不可回显或覆盖。最低实例变量：

- `DATABASE_URL`；
- `APP_URL`；
- `SESSION_SECRET`；
- `MASTER_ENCRYPTION_KEY`；
- `SETUP_TOKEN`（完成初始化后失效）；
- `DEPLOYMENT_MODE=personal|shared`；
- 可选邮件、代理和实例级 AI provider 变量。

### 27.6 健康检查、启动和恢复

- `/health/live` 只验证进程存活；
- `/health/ready` 验证数据库、迁移版本、主加密密钥和任务执行器；
- `/api/version` 返回应用、schema、prompt、rubric 和交换格式版本，不返回 secret；
- 首次迁移失败时保持 not-ready，并显示可执行错误；不能带着半迁移 schema 接受写作；
- 后台任务使用可恢复 lease；进程意外退出后任务能够被安全重新领取；
- 所有 AI 调用与回调保持幂等，恢复不能产生重复报告或重复费用；
- AI provider 不可用不影响应用 readiness，只影响相应任务状态。

### 27.7 备份、恢复与升级

- UI 和命令行均可创建备份；
- 标准备份包含数据库、上传文件、schema 版本和恢复清单，不包含明文 provider key；
- 如果备份要保留已加密 provider key，必须同时安全保留实例主密钥，或使用用户提供的备份口令重新加密；
- 恢复操作先验证校验和、版本兼容性和可用磁盘空间；
- CI 至少对前一个稳定版本执行一次备份—升级—恢复测试；
- release 前禁止无法回退且无迁移说明的破坏性 schema 变更；
- `docker compose pull && docker compose up -d` 是标准升级路径；应用自动运行向前迁移，但重大升级必须先提示备份；
- 云端模板必须说明平台自身数据库备份责任，项目同时保留可移植导出方案。

### 27.8 部署验收标准

1. 安装 Docker 的全新机器运行一条命令后，5 分钟内可以打开首次启动向导。
2. 本地默认端口只监听 loopback；显式配置后才能对局域网或公网开放。
3. 首次启动不要求命令行填写 AI key。
4. Railway 部署从点击按钮到打开向导不需要手工创建数据库或复制连接串。
5. 每个 release 的 Compose 与主要云模板都运行真实浏览器冒烟测试。
6. 应用重启、数据库短暂不可用和 AI 调用中断后，作文与任务均不会丢失或重复处理。
7. 删除应用容器不会删除命名数据卷；文档明确区分 stop、down 和删除 volume。
8. `amd64` 与 `arm64` 镜像启动、迁移和健康检查均通过。
9. 备份可以在干净实例恢复，并保留训练轮次、排程和能力档案。
10. 公开部署开启安全 cookie、CSRF 防护、登录限流和可信代理配置。

## 28. Codex Skill 交付

### 28.1 定位与交付边界

最终必须随 Web 项目交付一个独立、可安装的 `coach-ielts-writing` Skill。它面向不想使用网页、愿意在 Codex 对话和本地文件中完成训练的用户。

Skill 与 Web 在以下方面必须对齐：

- IELTS 四项评估结构与证据要求；
- 能力分类 `skill_id`；
- 一个核心新目标的专项课原则；
- 首次答案、提示和最终答案分离；
- 即时应用、保持和迁移的证据门槛；
- Version 2 前锁定完整范文；
- D1–D2、D5–D7、D14 的间隔逻辑；
- Version 1 / 2 对比与每百词问题频率；
- 可导入、导出的学习档案 schema。

Skill 不需要用户配置额外 AI API；语言诊断和教学由运行 Skill 的 Codex 完成。Skill 不依赖 Web 后端、数据库或项目维护者的服务器。

### 28.2 规范目录

Skill 源码放在仓库：

```text
.agents/skills/coach-ielts-writing/
├── SKILL.md
├── agents/
│   └── openai.yaml
├── references/
│   ├── assessment-rubric.md
│   ├── lesson-design.md
│   ├── workflow-and-mastery.md
│   ├── state-schema.md
│   └── exercise-contracts.md
└── scripts/
    ├── coach_state.py
    ├── validate_lesson.py
    ├── build_rewrite_packet.py
    ├── compare_attempts.py
    └── validate_state.py
```

- `SKILL.md` 保持精简，只保留核心流程、资源路由和不可违反的教学约束；
- 详细评分、课程、状态和题型规则放入按需读取的 references；
- 确定性建档、排程、泄露检查、状态转换和比较指标放入 scripts；
- Skill 目录内不增加 README、安装指南或 changelog；安装与发布说明位于项目根 README；
- `agents/openai.yaml` 提供显示名称、短描述和默认调用提示，但不声明 Web 或外部 MCP 为硬依赖。

### 28.3 触发范围

frontmatter 的 `description` 必须同时覆盖中英文触发，并明确边界。工作名称：

```yaml
---
name: coach-ielts-writing
description: Run a complete IELTS Writing Task 2 coaching cycle from timed first draft through evidence-based assessment, personalized active practice, delayed closed-book rewriting, comparison, and transfer checks. Use when the user asks in English or Chinese to practise, 批改, 重写, 复盘, continue, or track IELTS Task 2 writing. Do not use for Task 1, general translation, or isolated correction unless the user asks to add it to IELTS training.
---
```

应触发：开始 Task 2、批改整篇作文、生成专项训练、继续未完成课程、查看到期重写、比较 V1/V2、执行迁移检测和查看能力档案。

不应强制启动完整闭环：普通翻译、与 IELTS 无关的润色、Task 1，或只询问一句英语且没有要求纳入训练。处于活动轮次时可以提供“加入能力档案”的非阻塞选项。

### 28.4 Skill 对话流程

1. **启动或恢复**：先读取本地状态并计算唯一下一步；除非用户明确要求，不另开冲突轮次。
2. **Version 1**：保存题目、开始时间、提交时间、辅助与中断状态；提交后锁定原稿。
3. **批改**：保存四项估分、原文证据、置信度和版本；对话先显示一分钟摘要和唯一核心目标。
4. **专项课**：一次呈现一道任务；完成前测、最小讲解、修复、递减支架、无提示生成、即时近迁移、段落、自检和出门测；每次先保存首次答案再反馈。
5. **排程**：写入 D1–D2 重写、D5–D7 迁移检测和 D14 混合复测；需要时增加短回炉。
6. **Version 2**：生成只含题目和抽象检查项的 rewrite packet；推荐在新的 Codex 任务中重写，避免旧对话泄露答案。
7. **对比与迁移**：用同一评分版本比较两版；迁移题更换话题和表层词汇，不直接提示目标表达。

每次调用只突出一个主动作，不能把完整工作流一次性全部倾倒给用户。

### 28.5 本地状态与隐私

用户数据不得写入 Skill 安装目录。首次使用时选择学习 workspace；默认建议在当前专用学习目录下创建：

```text
.coach-ielts-writing/
├── manifest.json
├── profile.json
├── queue.json
├── ability-profile.json
├── evidence.jsonl
└── cycles/<cycle-id>/
    ├── cycle.json
    ├── question.md
    ├── attempt-v1.md
    ├── assessment.json
    ├── feedback.md
    ├── lesson-plan.json
    ├── responses.jsonl
    ├── rewrite-packet.md
    ├── attempt-v2.md
    ├── comparison.json
    └── transfer/
```

- 所有状态含 `schema_version`、时区、ISO 时间、当前阶段、版本、模型标识和判分置信度；
- 所有写入使用临时文件 + 原子替换、文件锁和乐观版本号；
- 作文正文不得写入 Mind memory、Skill 安装目录或普通日志；
- scripts 只修改明确的学习 workspace，不搜索或删除其他目录；
- 删除操作默认导出备份并要求用户明确确认目标轮次或 workspace；
- Web / Skill 交换包使用相同 schema，导入前验证版本和校验和。

### 28.6 脚本与模型的职责

脚本负责：

- 初始化、状态查询、合法状态迁移和唯一下一动作；
- 原子保存、暂停恢复、日期计算和队列；
- 校验课程时长、目标数量、主动输出比例和答案泄露；
- 生成只含白名单字段的 rewrite packet；
- 确定性计算字数、用时、每百词问题频率和复发指标；
- 校验 schema、文件引用、并发版本和损坏恢复。

Codex 负责：

- 理解作文含义；
- 依据 rubric 估分并引用原文证据；
- 判断搭配、自然度和论证机制；
- 在模板约束内生成练习和参考表达；
- 按量表评价开放句子与段落。

Codex 不得自由修改排程、状态、掌握门槛或本地 JSON；脚本不得决定语言是否自然或生成 IELTS 观点。

### 28.7 与 Web 无法完全等价的部分

- Skill 只能记录计时，不能像网页编辑器一样强制冻结 40 分钟；
- 当前对话历史无法真正隐藏，Version 2 应建议开启只读取 rewrite packet 的新 Codex 任务；
- 没有自动化能力时不能保证后台推送，只能在下次调用时显示到期任务；用户明确要求且 Codex 提供计划任务时，才创建提醒；
- 没有拖拽、单卡动画和完整仪表盘，以“一次一题、阶段标签和本地报告”替代；
- AI 分数仍是估分，必须带置信度、模型和 rubric 版本。

这些差异必须在 Skill 中诚实说明，不能声称与网页的计时、防泄露和通知控制完全相同。

### 28.8 安装与分发

- 仓库内 Skill 放在 `.agents/skills/coach-ielts-writing`，克隆项目后 Codex 可以按 repo scope 发现；
- 根 README 提供通过 `$skill-installer` 从公开 GitHub 仓库安装到用户 scope 的示例；
- release 同时附带只包含 Skill 必要目录的校验和压缩包；
- 独立 Skill 是必交付物；后续可以再包装为 plugin 方便分发，但 plugin 不作为首个 release 的功能前提；
- Skill 版本、交换 schema 和 Web release 建立兼容矩阵。

### 28.9 Skill 验证

实现时必须使用 `skill-creator` 提供的初始化与验证流程：

- 用 `init_skill.py` 建立规范目录；
- 生成并校验 `agents/openai.yaml`；
- 运行 `quick_validate.py`；
- 对所有 scripts 执行单元测试和临时目录集成测试；
- 校验所有 SKILL.md / reference 链接；
- 使用隔离状态目录做 fresh-agent forward tests。

至少覆盖以下 forward-test 场景：

1. 中文用户从零开始，只出现一个下一步且不提前展示范文；
2. 含 `much slighter pressure` 的作文能区分语法、搭配和表达视角；
3. 连续答错后按提示阶梯补救，并保留首次错误；
4. 中断超过 24 小时后先做无提示重新基线；
5. 新任务执行 Version 2 时 rewrite packet 不泄露 V1、反馈或范文；
6. 用户在重写中要求答案时正确标记为 assisted，不能产生保持证据；
7. 一次 D5–D7 成功不会被误标为最终掌握；
8. 普通翻译请求不会误启动完整课程；
9. 并发或损坏状态能安全恢复，不覆盖作文；
10. 切换模型或 rubric 版本后不会把分数差直接解释为能力变化。

Skill release gate：状态转换非法次数为 0、Version 2 答案泄露次数为 0、即时课程误标 `retained/transferred` 次数为 0、首次答案和提示依赖可追溯率为 100%。

### 28.10 Web / Skill 单一事实源

仓库维护一个不依赖 Web UI 或 Codex 对话的版本化 `learning-contracts/` 包，至少包含：

- 能力分类和 `skill_id`；
- IELTS 四项批改、课程、练习、证据事件、重写包和交换包的 JSON Schema；
- 合法状态迁移与掌握门槛；
- prompt / rubric / contract 版本清单；
- 去标识化 golden fixtures 和预期验证结果。

Web 运行时和 Skill scripts 都必须消费或由该包生成这些确定性协议，不能各自复制一套悄悄分叉的规则。Skill 中供模型阅读的 reference 可以由规范源生成，但 release 必须保存生成物并校验 checksum，确保安装 Skill 后不依赖 Web 项目运行。

每个 release 的兼容测试至少覆盖：Web 导出 → Skill 导入 → Skill 更新 → Web 重新导入，以及相反方向。不同评分模型造成的语言判断差异必须记录为模型和 rubric 版本差异，不能被误判为 schema 不兼容或用户能力变化。

## 29. 开源 / BYOAI / 部署 / Skill 发布门槛

在项目能够被称为第一个公开可用版本前，必须同时满足：

1. 公共仓库、开源许可证和贡献 / 安全文件完整；
2. 全新环境无需私有依赖即可从源码构建；
3. `docker compose up -d` 和主要云端部署模板通过 E2E；
4. 无 AI key 时应用仍可启动、保存写作和运行 demo；
5. UI 可以完成 OpenAI 与 compatible endpoint 的测试和保存；
6. 浏览器、API、日志、错误追踪和导出均无法取回明文 key；
7. 自定义 endpoint 的 SSRF 防护通过安全测试；
8. AI provider 更换不需要修改学习业务代码；
9. 等待中的 AI 任务可以在修复配置后幂等恢复；
10. 备份能够在干净实例恢复；
11. Web 与 Skill 交换包双向导入通过；
12. `coach-ielts-writing` 通过格式验证、脚本测试和 fresh-agent forward tests；
13. README 同时提供 Web 自托管和只安装 Skill 两条最短路径；
14. 所有公开承诺均能由自动测试、版本化样例或可重复人工验收证明。

## 30. 官方参考

- OpenAI / Codex Skills：<https://learn.chatgpt.com/docs/build-skills>
- OpenAI API authentication and API-key handling：<https://developers.openai.com/api/reference/overview#authentication>
- Docker Compose production guidance：<https://docs.docker.com/compose/how-tos/production/>
- Railway templates：<https://docs.railway.com/templates/deploy>
- Render Blueprint specification：<https://render.com/docs/blueprint-spec>
